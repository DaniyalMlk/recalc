{
  "name": "recalc",
  "version": "0.1.0",
  "description": "A spreadsheet formula engine in TypeScript: lexer, parser, dependency graph and incremental recalculation.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "recalc": "dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "repl": "tsx src/cli.ts"
  },
  "keywords": [
    "spreadsheet",
    "formula",
    "parser",
    "dependency-graph",
    "typescript"
  ],
  "license": "MIT",
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
